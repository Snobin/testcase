import { Resizabledirective } from './resizabledirective';

describe('Resizabledirective', () => {
  it('should create an instance', () => {
    expect(new Resizabledirective()).toBeTruthy();
  });
});
